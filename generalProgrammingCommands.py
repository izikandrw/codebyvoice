from dragonfly import *

map = {
    "brax":                     Text("{}") + Key("left"),
    "parens":                   Text("()") + Key("left"),
    "angles":                   Text("<>") + Key("left")
}
